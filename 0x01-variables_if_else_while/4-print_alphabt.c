#include <stdio.h>

/**
 *  * main - Entry point
 *   *  
 *    * Return: Always 0 (Success)
 *     */
int main(void)
{
	    char letter;

	        for (letter = 'a'; letter <= 'z'; letter++) // Loop through lowercase letters
							    //     {
							    //             if (letter != 'q' && letter != 'e') // Skip 'q' and 'e'
							    //                     {
							    //                                 putchar(letter); // Print each letter except 'q' and 'e'
							    //                                         }
							    //                                             }
							    //                                                 putchar('\n'); // Print a newline character
							    //
							    //                                                     return (0);
							    //                                                     }
							    //
