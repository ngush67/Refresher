#include <stdio.h>

/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 *     */
int main(void)
{
	    int digit;

	        for (digit = 0; digit < 10; digit++) // Loop through digits 0 to 9
						     //     {
						     //             putchar(digit + '0'); // Convert digit to its ASCII equivalent and print
						     //                 }
						     //                     putchar('\n'); // Print a newline character
						     //
						     //                         return (0);
						     //                         }
						     //
